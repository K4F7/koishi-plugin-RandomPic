{
  "name": "koishi-plugin-random-pic",
  "version": "0.1.0",
  "description": "A Koishi plugin that sends random images from configured galleries.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "koishi",
    "plugin",
    "random",
    "image"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "koishi": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
